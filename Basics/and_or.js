let firstName ="akhil";
let age=23;

if(firstName[0] === 'a' && age>=20) //instead of && operator we use || operator
{
    console.log("inside if loop"); 
}else{
    console.log("inside else loop");
}